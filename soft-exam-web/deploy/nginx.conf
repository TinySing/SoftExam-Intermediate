# 将 example.com 改成你的实际域名；没有域名时可先用服务器 IP 访问。
server {
    listen 80;
    server_name example.com;
    root /var/www/soft-exam;
    index index.html;

    location / { try_files $uri $uri/ /404.html; }
}
